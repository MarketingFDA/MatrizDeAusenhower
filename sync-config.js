/* =========================================================================
   Configuração da sincronização compartilhada (Matriz de Eisenhower — Fradema)

   url: endereço do Web App do Google Apps Script que guarda o estado do time.
        Cole aqui a URL de implantação (termina em /exec), por exemplo:
        url: "https://script.google.com/macros/s/AKfycb.../exec"

   Deixe a url VAZIA ("") para o app funcionar 100% local (localStorage do
   navegador), exatamente como antes: sem chave, sem modal, sem rede.
   ========================================================================= */
window.MATRIZ_SYNC = { url: "" };
